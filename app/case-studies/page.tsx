import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, Buildings, CheckCircle } from "@phosphor-icons/react/dist/ssr";
import { MarketingFooter } from "@/components/marketing-footer";
import { MarketingHeader } from "@/components/marketing-header";
import { caseStudies } from "@/lib/resources-data";
export const metadata: Metadata = { title: "Case Studies", description: "Modeled operating studies showing how PRIFYN turns growth signals into accountable decisions." };
export default function CaseStudiesPage() { return <div className="marketing-page"><div className="subpage-header"><MarketingHeader /></div><main><section className="resource-hero case-hero"><div className="marketing-container"><span className="section-kicker">Operating studies</span><h1>See the decision,<br /><em>not just the dashboard.</em></h1><p>These modeled studies show how PRIFYN is designed to handle validated growth and operating problems across ambitious businesses. They are product scenarios, not fabricated customer claims.</p></div></section><section className="section section-paper"><div className="marketing-container case-grid">{caseStudies.map((study, index) => <article className="case-card surface" key={study.slug}><div className={`case-art case-art-${index + 1}`}><Buildings weight="duotone" /><span>{study.industry}</span></div><div><span className="case-label"><CheckCircle weight="fill" />{study.label}</span><h2>{study.title}</h2><p>{study.summary}</p><div className="case-outcomes">{study.outcome.map(item => <span key={item}>{item}</span>)}</div><Link className="text-link" href={`/case-studies/${study.slug}`}>Read operating study <ArrowRight /></Link></div></article>)}</div></section></main><MarketingFooter /></div>; }
