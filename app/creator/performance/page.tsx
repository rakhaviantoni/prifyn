import { CreatorLivePerformance } from "@/components/metrics/live-metrics";

export default function CreatorPerformancePage() {
  return <div className="app-content"><header className="app-page-head"><div><span>Performance intelligence</span><h1>What is improving.</h1><p>Creator results use the same workspace evidence layer: affiliate links, coupon orders, imported exports, campaign proof, and connected platform metrics.</p></div></header><CreatorLivePerformance /></div>;
}
