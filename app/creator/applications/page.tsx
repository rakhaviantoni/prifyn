import { ArrowRight, Clock, CheckCircle, PaperPlaneTilt } from "@phosphor-icons/react/dist/ssr";
const applications = [["Weekday Lunch Reset", "Dapur Saji", "Shortlisted", "Brand reviewing your rate", "shortlisted"], ["Morning Rituals", "Kopi Pulang", "Applied", "Submitted 1 Aug 2026", "applied"], ["Kitchen Confidence", "Rasa Rumah", "Selected", "Agreement ready to review", "selected"]];
export default function ApplicationsPage() { return <div className="app-content"><header className="app-page-head"><div><span>Application tracking</span><h1>Applications</h1><p>Know exactly where each opportunity stands and what action comes next.</p></div></header><section className="surface application-list">{applications.map(([title, brand, status, next, tone]) => <article key={title}><span className={`application-icon ${tone}`}>{status === "Selected" ? <CheckCircle weight="fill" /> : status === "Applied" ? <PaperPlaneTilt /> : <Clock />}</span><div><span>{brand}</span><h2>{title}</h2><p>{next}</p></div><span className={`status-pill ${status === "Selected" ? "" : "neutral"}`}>{status}</span><button className="icon-button" type="button" aria-label={`Open ${title}`}><ArrowRight /></button></article>)}</section></div>; }
