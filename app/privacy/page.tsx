import type { Metadata } from "next";
import { LegalPage } from "@/components/legal-page";
export const metadata: Metadata = { title: "Privacy" };
export default function PrivacyPage() { return <LegalPage title="Privacy by design" intro="This product-preview policy describes the controls PRIFYN is being built around. It is not a substitute for the final jurisdiction-specific policy before commercial launch." sections={[["Data minimization", "PRIFYN collects only the identity, workspace, campaign, creator, and performance information required to provide the selected workflows."], ["AI processing", "AI providers receive curated, permission-filtered evidence bundles. Secrets and unnecessary personal information are excluded. AI cannot independently change business records."], ["Control and deletion", "Workspace owners will be able to export and request deletion of tenant data subject to security, legal, and audit-retention requirements."], ["Security", "Tenant boundaries, field-level permissions, signed asset access, audit events, encryption in transit, and production monitoring form the baseline control set."]]} />; }
